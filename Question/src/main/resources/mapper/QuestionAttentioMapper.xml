<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.question.dao.QuestionAttentionDao">
    <insert id="addNewAttention">
        insert into question_attention(userId,questionId) values (#{userId},#{questionId})
    </insert>
    <select id="getIdByQuestionIdUserId" resultType="java.lang.Long">
        select id from question_attention where questionId = #{questionId} and userId = #{userId} limit 0,1
    </select>
    <delete id="removeAttention">
        delete from question_attention where userId = #{userId} and questionId = #{questionId}
    </delete>
</mapper>