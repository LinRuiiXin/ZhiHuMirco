<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.user.dao.AttentionDao">
    <select id="userHasAttention" resultType="java.lang.Long">
        select id from attention where userId = #{userId} and beAttentionUserId = #{targetUserId}
    </select>
    <insert id="follow">
        insert into attention(userId,beAttentionUserId)
        values (#{userId},#{targetUserId})
    </insert>
    <delete id="unFollow">
        delete from attention
        where userId = #{userId} and beAttentionUserId = #{targetUserId}
    </delete>
</mapper>